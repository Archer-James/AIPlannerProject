name: Pylint Testing

on:
  push:
    branches:
      # - canvas_connect
      - '**'
  pull_request:
    branches:
      - main
    paths:
      - 'AIPlanner/AIPlanner/**/*.py'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pylint
    - name: Analysing the code with pylint
      run: |
        pylint --rcfile=pylintrc AIPlanner/AIPlanner/**/*.py
        
        #$(git ls-files '*.py')


# name: Python Linting with Pylint

# on:
#   pull_request:
#     paths:
#       - '**/*.py'
#   push:
#     branches:
#       - main
#       # - 'feature/*'

# jobs:
#   lint:
#     runs-on: ubuntu-latest

#     steps:
#       # Check out the code
#       - name: Checkout code
#         uses: actions/checkout@v2

#       # Set up Python
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.x'  # Specify the version you want to use

#       # Install dependencies
#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install pylint

#       # Run pylint with the custom pylintrc
#       - name: Run Pylint with custom pylintrc
#         run: |
#           pylint --rcfile=pylintrc **/*.py
